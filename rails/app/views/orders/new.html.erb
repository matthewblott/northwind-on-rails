<% content_for :title, "New Order" %>

<controls class="turbo-native-hidden">
  <%= button_tag "Create", form: "order_form",
    formaction: create_order_path,
    data: { controller: 'bridge--save', bridge__save_target: "submit" },
    method: :patch %>
  <%= link_to "Back", index_order_path, role: "button" %>
</controls>

<%= render "form", order: @order, disabled: false %>
