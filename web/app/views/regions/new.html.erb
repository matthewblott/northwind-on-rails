<% content_for :title, "New Region" %>

<controls class="turbo-native-hidden">
  <%= button_tag "Create", form: "region_form",
    formaction: create_region_path,
    data: { controller: 'bridge--save', bridge__save_target: "submit" },
    method: :post %>
  <%= link_to "Back", index_region_path, role: "button" %>
</controls>

<%= render "form", region: @region, disabled: false %>
