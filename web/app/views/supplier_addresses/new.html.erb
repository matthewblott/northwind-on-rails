<% content_for :title, "New Supplier Address" %>

<controls class="turbo-native-hidden">
  <%= button_tag "Create", form: "address_form",
    formaction: create_supplier_address_path(params[:supplier_id]),
    data: { controller: 'bridge--save', bridge__save_target: "submit" },
    method: :patch %>
  <%= link_to "Back", index_supplier_address_path(params[:supplier_id]), role: "button" %>
</controls>

<%= render "address_form", address: @address %>
